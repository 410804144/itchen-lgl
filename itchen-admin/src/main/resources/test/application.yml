server:
  port: 10001
  servlet:
    context-path:
  shutdown: graceful

spring:
  profiles:
    active: test, service

common:
  swagger-open: true

jwt-secret: tBDYEeqHmFR65ZVO # JWT密钥

logging:
  file:
    path: /usr/local/share/applications/itchen-admin