server:
  port: 10000
  servlet:
    context-path:
  shutdown: graceful

spring:
  profiles:
    active: test, service

common:
  swagger-open: true

jwt-secret: mFsCOvZHWpmJRGvj # JWT密钥

logging:
  file:
    path: /usr/local/share/applications/itchen-api